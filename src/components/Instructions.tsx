import { Stack } from "@mui/material";

const Instructions: React.FC = () => {
  return (
    <Stack spacing={2} className="instructions">
      <h1>
        <p>Testmod enabled, enter props</p>
        <p>Тут може бути інструкція з використання динамичних данних</p>
      </h1>
    </Stack>
  );
};

export default Instructions;
